<h1>ユーザー一覧</h1>
<% @users.each do |user| %>
  <div>
    <%= link_to admin_user_path(user.id) do %>
      <%= image_tag user.get_image %>
      <%= user.name %>
    <% end %>
   <!--   < if user.is_active? %>-->
			<!--	<span class="text-active">有効</span>	-->
			<!--< else %>-->
			<!--	<span class="text-invalid">利用停止中</span>-->
			<!--< end %>-->
  </div>
<% end %>
