<h1>プロフィールを選択</h1>
<%= form_with model: @user, url: user_path(@user.id), method: :patch, local: true do |f| %>
  <%= f.label :name, "プロフィール画像"%>
  <%= f.file_field :image, accept: "image/*" %>
  <%= f.label :name, "名前" %>
  <%= f.text_field :name %>
  <%= f.label :introduction, "自己紹介"%>
  <%= f.text_area :introduction %>
  <%= f.submit '保存する' %>
<% end %>